source 'https://rubygems.org'

ruby '3.0.0' # Specify the ruby version

# Specify your gem dependencies here
gem 'sinatra'
gem 'sinatra-contrib'
gem 'rackup'
gem 'json'

group :development do
  gem 'sinatra-reloader' # require the sinatra reloader gem if in development mode
end
